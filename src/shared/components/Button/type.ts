export interface ButtonCustomeProps {
  title: string;
  buttonColor: string;
  titleColor: string;
  size: string;
  onPress: any;
  type: string;
  disable: boolean;
}

export interface ButtonPrimaryProps {
  title: string;
  buttonColor: string;
  titleColor: string;
  size: string;
  onPress: any;
}
